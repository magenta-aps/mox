{% macro bvnlist(list) -%}
{% if list %}
    {% set comma=joiner(",") %}
    {% for item in list.current %}
        {% if item.uuid and item.current.brugervendtnoegle -%}
             {{ comma() }}{{ item.current.brugervendtnoegle }}_{{ item.id }}
        {%- elif item.urn -%}
            {{ comma() }}{{ item.urn }}
        {%- endif %}
    {% endfor %}
{% endif %}
{%- endmacro %}